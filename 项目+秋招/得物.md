业务概述：实现场域(会场/频道)投放(权益、商品流个性化定制)诉求。解决投放领域(投放、投承一体)链路闭环，  
最终实现支持招选搭投的投放能力闭环。我主要负责会场相关业务的建设和迭代。   
个人产出： 

1. 会场诊断机器人开发：使用模版方法+策略模式构建飞书机器人问答能力，增强其可扩展性，支持不同问题的回答。 
2. 会场诊断原因优化：主要实现了商品掉楼和商品优惠提示优化  
通过 OpenFeign 对接下游查询，通过 Stream 进行数据过滤，分析并透出商品配置数据同步问题和同质商品问题。  
优惠诊断原因提示优化：通过方法重载 + Consumer 参数适配原有方法，在不影响其他方法情况下完成提示优化。
3. 会场大盘与告警治理：原会场埋点数据逻辑与大盘告警逻辑不一致，导致告警频繁。通过逻辑梳理和 sql 重写完成  
5 个大盘绘制，1 个告警治理，实现 0 错误率；  




## 业务的理解
<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/51149297/1760190521279-0a5622af-36bb-4293-b10a-3aae975440b5.png)

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/51149297/1760190837620-3e3c13ea-4385-414f-b533-447b4f9b0ef7.png)

## 实习的一些
冒烟点--即不处理会对线上造成故障或系统中存在一定的风险问题，比如：某线上服务接口rt间断性不明原因升高、商品推荐重复等  
灰度：  
为了解决业务高复杂情况下的快速迭代的稳定性，需要支持灰度发布，即新版本的服务上线后发少数实例，将特定的用户、特定的IP的请求强制流入这部分少数实例做验证，待验证通过以后再发全量。在其上可以进行A/B testing，即让一部分用户继续用产品特性A，一部分用户开始用产品特性B，如果用户对B没有什么反对意见，那么逐步扩大范围，把所有用户都迁移到B上面来。灰度发布可以保证整体系统的稳定，在初始灰度的时候就可以发现、调整问题，以保证其影响度。  
蓝绿发布：  
蓝绿部署中，一共有两套系统：一套是正在提供服务系统(也就是旧版)，标记为“绿色”；另一套是准备发布的系统，标记为“蓝色”。两套系统都是功能完善的，并且正在运行的系统，只是系统版本和对外服务情况不同。  
蓝色系统不对外提供服务，用来做啥？  
用来做发布前测试，测试过程中发现任何问题，可以直接在蓝色系统上修改，不干扰用户正在使用的系统。  
蓝色系统经过反复的测试、修改、验证，确定达到上线标准之后，直接将用[https://poizon.feishu.cn/wiki/wikcnZj4o1odBtqrQpn9o6OzJvz户切换到蓝色系统](https://poizon.feishu.cn/wiki/wikcnZj4o1odBtqrQpn9o6OzJvz户切换到蓝色系统), 切换后的一段时间内，依旧是蓝绿两套系统并存，但是用户访问的已经是蓝色系统。这段时间内观察蓝色系统（新系统）工作状态，如果出现问题，直接切换回绿色系统。  
当确信对外提供服务的蓝色系统工作正常，不对外提供服务的绿色系统已经不再需要的时候，蓝色系统正式成为对外提供服务系统，成为新的绿色系统。 原先的绿色系统可以销毁，将资源释放出来，用于部署下一个蓝色系统。

组件与展位

+ 一个组件类型直接映射到一个展位
+ 一个组件内有 n 中交互，需要映射到 n 个展位上
+ 一个组件内有 n 个分发数据源，需要映射到 n 个展位上

一些名词  
展位：展位是可以跳转到频道或者会场的流量引导坑位入口，例如分类tab等。展位包含了从召回、补全到展示的所有后端能力模块；  
单元：比展位小一个层级的模块。包括两种：权益投放活动，投放商品流；  
素材：素材本质上指的是投放相关的资源，但不是单元的内容，单元是投放查询返回的；  
计划：给予以上投放模型以时间维度的扩展，不同时间可以对应到不同的投放实体；  
规则：投放具有个性化配置能力，根据不同的条件，如人群、ab实验等投放的过滤和排序规则，都会有不同。

钩子：和用户互动交互的具体方法，可以是一个弹窗、一个发券等；  
人群：不同的人群有不同特征和购买习惯，以及不同的营销成长策略；  
渠道：指的是具体的有流量的业务或者位置，需要把渠道-钩子-人群三者关系串起来，达到针对特定人群应用最优营销策略的目标。



## 会场机器人
1. 会场查询一些东西，机器人输入对应的功能编号，就做出对应的回应
2. 调试是抓包的

策略工厂，ApplicationContext 给注入进去， applicationContext.getBean(beanName, DiagnosticStrategy.class);

使用的是<font style="color:rgb(249, 250, 251);background-color:rgb(21, 21, 23);">@PostConstruct + Map</font>是， @PostConstruct确保在Bean属性注入之后执行，确保策略的Bean准备就绪。

优化：

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/51149297/1760193452518-00b8cec6-0638-4480-b5e5-8df985381780.png)

```java
@Component
public class DiagnosticStrategyFactory {
    
    @Autowired
    private ApplicationContext applicationContext;
    
    /**
     * 策略映射表：策略编码 -> Bean名称
     */
    private final Map<String, String> strategyMapping = new ConcurrentHashMap<>();
    
    /**
     * 实例缓存：策略编码 -> 策略实例
     */
    private final Map<String, DiagnosticStrategy> instanceCache = new ConcurrentHashMap<>();
    
    /**
     * 初始化策略映射关系
     */
    @PostConstruct
    public void init() {
        System.out.println("开始初始化诊断策略映射...");
        
        // 获取所有DiagnosticStrategy的实现类
        Map<String, DiagnosticStrategy> strategies = applicationContext.getBeansOfType(DiagnosticStrategy.class);
        
        for (Map.Entry<String, DiagnosticStrategy> entry : strategies.entrySet()) {
            String beanName = entry.getKey();
            DiagnosticStrategy strategy = entry.getValue();
            String strategyCode = strategy.getStrategyCode();
            
            // 检查策略编码是否重复
            if (strategyMapping.containsKey(strategyCode)) {
                throw new IllegalStateException("策略编码重复: " + strategyCode);
            }
            
            // 保存映射关系
            strategyMapping.put(strategyCode, beanName);
            System.out.println("注册策略: " + strategyCode + " -> " + strategy.getDescription());
        }
        
        System.out.println("诊断策略映射初始化完成，共注册 " + strategies.size() + " 个策略");
    }
    
    /**
     * 获取策略实例（懒加载）
     */
    public DiagnosticStrategy getStrategy(String strategyCode) {
        // 检查编码是否存在
        if (!strategyMapping.containsKey(strategyCode)) {
            throw new IllegalArgumentException("未知的策略编码: " + strategyCode);
        }
        
        // 从缓存获取，如果没有则创建
        return instanceCache.computeIfAbsent(strategyCode, code -> {
            String beanName = strategyMapping.get(code);
            return applicationContext.getBean(beanName, DiagnosticStrategy.class);
        });
    }
    
    /**
     * 获取所有策略信息
     */
    public Map<String, String> getAllStrategies() {
        Map<String, String> result = new LinkedHashMap<>();
        strategyMapping.forEach((code, beanName) -> {
            DiagnosticStrategy strategy = getStrategy(code);
            result.put(code, strategy.getDescription());
        });
        return result;
    }
    
    /**
     * 清除缓存（用于重新加载）
     */
    public void clearCache() {
        instanceCache.clear();
        System.out.println("策略缓存已清除");
    }
    
    /**
     * 重新加载单个策略
     */
    public void reloadStrategy(String strategyCode) {
        instanceCache.remove(strategyCode);
        System.out.println("策略缓存已清除: " + strategyCode);
    }
}
```







```java
@Component
public class SmartStrategyFactory implements ApplicationContextAware {
    
    private ApplicationContext applicationContext;
    
    // ✅ 使用延迟加载的缓存
    private final Map<String, DiagnosticStrategy> strategyCache = new ConcurrentHashMap<>();
    
    @Override
    public void setApplicationContext(ApplicationContext applicationContext) {
        this.applicationContext = applicationContext;
    }
    
    public DiagnosticStrategy getStrategy(String beanName) {
        return strategyCache.computeIfAbsent(beanName, name -> {
            // ✅ 延迟加载：只有真正用到时才从Spring容器获取
            try {
                return applicationContext.getBean(name, DiagnosticStrategy.class);
            } catch (NoSuchBeanDefinitionException e) {
                throw new RuntimeException("策略Bean未找到: " + name, e);
            }
        });
    }
    
    // ✅ 支持动态注册新策略
    public void registerStrategy(String beanName, DiagnosticStrategy strategy) {
        strategyCache.put(beanName, strategy);
    }
    
    // ✅ 手动清除缓存（用于热更新）
    public void evictCache(String beanName) {
        strategyCache.remove(beanName);
    }
}
```





<font style="color:rgb(249, 250, 251);background-color:rgb(21, 21, 23);">我设计的是一个基于模板方法模式+策略模式的飞书机器人诊断系统，核心目标是让用户通过简单的数字指令就能完成复杂的会场业务诊断。"</font>

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/51149297/1760192682845-66a0ca9a-8629-4a1d-aa66-e14039a794c6.png)

## 会场诊断
### 一
会场组件配置spu数据的时候，会提交这批品的数据，有个性化的时候，回到千川那边，但是同步会存在一些延迟，大概两小时，这种情况需要从千川那边查询一下，看下最新同步时间是否大于修改时间，小于说明还没同步过去，就需要透出这个提示，此外，自己选的商品可能会存在同质化商品，同一个SPU，不同规格之类的，如果同步了，数据展示是从千川那边查询，千川会过滤掉同质化商品，前端这边就不会展示这些同质化商品，就会给运营造成困惑为什么没有展示，所以在从千川那边透出一下当前配置数据的spu，哪些是同质化的。看透出是否同步，透出配置的spu哪些是同质化的。

#### stream 特点
+ stream只能被消费一次，消费后不能被重复使用，可以链式调用，中间操作不会立即执行，只有终端操作会触发

#### 操作形态
+ 创建流。

```java
List<String> list = Arrays.asList("a", "b", "c");
Stream<String> stream1 = list.stream();          // 顺序流
Stream<String> stream2 = list.parallelStream();  // 并行流

String[] array = {"a", "b", "c"};
Stream<String> stream1 = Arrays.stream(array);
Stream<String> stream2 = Stream.of("a", "b", "c");
```



+ 中间操作

filter:过滤，去重，limit限制，skip跳过

映射转换，map,maptoInt,

排序

+ 终端操作

colllect:集合

```java
stream.collect(Collectors.toList());stream.collect(Collectors.toSet());
toArray
迭代：stream.forEach(System.out::println);           // 无序（并行流）
stream.forEachOrdered(System.out::println);    
```

#### 中间操作和中断操作区别
```java
List<String> result = list.stream()           // 创建流
    .filter(s -> s.length() > 3)             // 中间操作（不立即执行）
    .map(String::toUpperCase)                // 中间操作（不立即执行）
    .collect(Collectors.toList());           // 终端操作（触发执行）


Map<DiscountType, List<Product>> productsByDiscount = products.stream()
        .filter(p -> p.getDiscount() != null)
        .collect(Collectors.groupingBy(Product::getDiscountType));
```

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/51149297/1760249228954-51468d94-e687-40ab-bc34-55ece32e35e6.png)

#### 并行流原理
<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/51149297/1760249893882-f34c3613-f915-492c-b1f0-3087ab2ab15c.png)

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/51149297/1760249902430-321bbfbe-d212-4f02-bc75-61d0e124bceb.png)

