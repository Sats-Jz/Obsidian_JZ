

+ Vue2: OptionsApi: 
+ Vue3: 配置式API

> setup
>

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/51149297/1764253349098-34f78a3d-b08c-466f-b2f3-e5287b095902.png)



ref包裹基本数据类型, 需要.value

reactive包括对象， ref包裹对象是一个proxy

=== 具备响应式能力

+ toRefs(对象) 对象所有属性
+ toRef(基本)



# 2025/11/29
> setup 语法糖
>

```javascript
<script setup> 
// setup 允许书写组合式API
// 不在推荐this,打印this为Undefined
// 语法糖
import HelloWorld from './components/HelloWorld.vue'
</script>
<!-- <script>

  setup(){
    // 数据
    const msg = 'Hello Vue 3 with Composition API'
    // 函数
    const change = () => {
      console.log('change')
    }
    return{
      msg,
      change
    }
  }

</script> -->
```

> reactive和ref函数
>

+ reactive接受响应式的对象返回一个响应式对象
+ ref接受普通变量

```javascript
import {reactive} from 'vue'
  import {ref} from 'vue'
  const state = reactive(
    {
      count: 0
    }
  )
  const cnt = ref(0)

  const change = () => {
    state.count++
    console.log(state.count)
  }
  const change2 = () => {
    cnt.value++
    console.log(cnt.value)
  }
```



> computed
>

> watch
>

执行watch函数监听要变化产生的行为，监听的使ref对象

```javascript
watch(cnt, (newVal, oldVal) => {
  msg2.value = `count changed from ${oldVal} to ${newVal}`
})
```

```javascript
watch([cnt, state], ([newCnt, newState], [oldCnt, oldState]) => {
  msg3.value = `cnt changed from ${oldCnt} to ${newCnt}, state.count changed from ${oldState.count} to ${newState.count}`
})
```

> deep
>

watch监听的ref是浅层监听， 直接修改嵌套的对象属性不会触发回调执行



# 2025/11/30
```css
// ::v-deep() { // 样式穿透 /deep/vue2使用
//   .van-nav-bar {
//     &__arrow {
//       font-size: 18px;
//       color: var(--cp-text1);
//     }
//     &__text {
//       font-size: 15px;
//     }
//   }
// }
控制子组件样式使用样式穿透 ，加了scoped会使用穿透
```



```css
// scss可以定义变量 => 语法 $变量名：css变量值
// $font-color: #409eff;
// .login {
//   padding: 20px;
//   background-color: #f5f5f5;
//   van-button {
//     color: $font-color;
//   }
// }

// 原生css变量定义，不需要打包浏览器默认支持
// 定义 --css变量名：css变量值
// 使用 var(--css变量名)
// 作用域：全局和局部
// 全局变量 :root{ }  |   局部变量 : .类名{ }
```

<!-- 这是一张图片，ocr 内容为： -->
![](https://cdn.nlark.com/yuque/0/2025/png/51149297/1764501478280-5db16a39-5e86-4a9a-b34e-04b34ca54482.png)

### 自定义组件属性提示
+ 定义dts文件

```typescript
import JzNavBar from "@/components/jz-nav-bar.vue";
declare module 'vue' {
    interface GlobalComponents {
        JzNavBar: typeof JzNavBar;
    }
}
```

# 一些问题
## 爆红，路径问题
参考<font style="color:rgb(34, 34, 38);">vue3+vite+typescript+vscode使用@alias路径别名配置不正确导致红色波浪线的解决办法</font>

[https://blog.csdn.net/snans/article/details/143361702](https://blog.csdn.net/snans/article/details/143361702)

原因覆盖问题

+ tsconfig.json

```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" } // 被覆盖
  ],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"] // 添加的
    }
  
}
}
```

+ tsconfig.app.json

```json
{
  "extends": "@vue/tsconfig/tsconfig.dom.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "types": ["vite/client"],

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true,
    "baseUrl": ".", //也添加上
    "paths": {
      "@/*": ["src/*"] //野田叫上
    }
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue"]
}
```

+ vite.config.ts

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import path from 'path';
// https://vite.dev/config/
export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```



## ￥attr
父组件的元素如果子组件没有被接受，那么就会挂载到子组件最外层属性上，如果自己组建最外层有何父组件传递的key一样的就会被覆盖



























